import React from "react"
import Layout from "../components/layout"

const Media = ({data}) => (
  <Layout>
    <div>
        <h1>
          Things I build:
        </h1>

        <h1>
          Things I talk about:
        </h1>
      </div>
  </Layout>
)

export default Media
